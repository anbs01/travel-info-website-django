@page "{id:int}"
@model TravelPortal.Web.Pages.tpco.Transports.EditModel
@{
    ViewData["Title"] = "编辑交通点";
}

<div class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden min-h-[calc(100vh-120px)]">
    <partial name="_AdminPageHeader" model='new { 
        Tabs = new[] {
            new { Name = "交通管理", Url = Url.Page("Index"), IsActive = false },
            new { Name = "编辑交通点", Url = "#", IsActive = true }
        }
    }' />

    <form method="post" class="p-8 space-y-8">
        <input type="hidden" asp-for="Transport.Id" />
        <input type="hidden" asp-for="Transport.CreatedAt" />

        <div class="grid grid-cols-1 gap-8">
            <div class="space-y-6">
                <div>
                    <label class="block text-xs font-bold text-gray-500 mb-2 uppercase tracking-wider">交通点名称</label>
                    <input asp-for="Transport.Title" class="w-full px-4 py-3 bg-gray-50 border-transparent rounded-xl focus:bg-white focus:ring-2 focus:ring-brand/20 focus:border-brand transition-all text-sm font-medium" required />
                </div>

                <div>
                    <label class="block text-xs font-bold text-gray-500 mb-2 uppercase tracking-wider">交通类型</label>
                    <select asp-for="Transport.TransportType" class="w-full px-4 py-3 bg-gray-50 border-transparent rounded-xl focus:bg-white focus:ring-2 focus:ring-brand/20 focus:border-brand transition-all text-sm font-medium">
                        <option value="Airport">机场</option>
                        <option value="Railway">火车站</option>
                        <option value="Bus">汽车站</option>
                        <option value="Pier">码头</option>
                    </select>
                </div>

                <div>
                    <label class="block text-xs font-bold text-gray-500 mb-2 uppercase tracking-wider">关联城镇</label>
                    <partial name="_GeoPicker" model='new { FieldName = "Transport.GeoId", CurrentId = Model.Transport.GeoId, CurrentName = Model.CurrentGeoTitle }' />
                </div>
            </div>

            <div class="space-y-6">
                <div>
                    <label class="block text-xs font-bold text-gray-500 mb-2 uppercase tracking-wider">联系电话</label>
                    <input asp-for="Transport.ContactPhone" class="w-full px-4 py-3 bg-gray-50 border-transparent rounded-xl focus:bg-white focus:ring-2 focus:ring-brand/20 focus:border-brand transition-all text-sm font-medium" />
                </div>

                <div>
                    <label class="block text-xs font-bold text-gray-500 mb-2 uppercase tracking-wider">详细地址</label>
                    <input asp-for="Transport.Address" class="w-full px-4 py-3 bg-gray-50 border-transparent rounded-xl focus:bg-white focus:ring-2 focus:ring-brand/20 focus:border-brand transition-all text-sm font-medium" />
                </div>

                <div>
                    <label class="flex items-center gap-3 cursor-pointer">
                        <input type="checkbox" asp-for="Transport.IsHidden" class="w-4 h-4 rounded border-gray-300 text-brand" />
                        <span class="text-xs font-bold text-gray-500 uppercase tracking-wider">屏蔽显示</span>
                    </label>
                </div>

                <div>
                    <label class="block text-xs font-bold text-gray-500 mb-2 uppercase tracking-wider">排序 (默认1000)</label>
                    <input type="number" asp-for="Transport.SortOrder" class="w-full px-4 py-3 bg-gray-50 border-transparent rounded-xl focus:bg-white focus:ring-2 focus:ring-brand/20 focus:border-brand transition-all text-sm font-mono" />
                </div>
            </div>
        </div>

        <div class="pt-8 border-t border-gray-50 flex justify-end space-x-4">
            <button type="submit" class="px-12 py-4 bg-brand text-white rounded-2xl text-sm font-bold shadow-xl shadow-brand/30 hover:scale-105 active:scale-95 transition-all">
                提交
            </button>
            <button type="reset" class="px-12 py-4 bg-gray-100 text-gray-500 hover:text-gray-700 hover:bg-gray-200 rounded-2xl text-sm font-bold hover:scale-105 active:scale-95 transition-all">
                重置
            </button>
        </div>
    </form>
</div>
